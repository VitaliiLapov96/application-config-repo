#Spring configuration
server:
  port: 8761

#Eureka configuration
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
    
#Actuator configuration
management:
  endpoints:
    web:
      exposure:
        include: health
